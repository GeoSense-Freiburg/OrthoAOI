# Default configuration for AOI segmentation with DINOv3
train_images: data/splits/train.csv
train_masks: data/splits/train.csv
val_images: data/splits/val.csv
val_masks: data/splits/val.csv
predict_images: null
predict_output: predictions

backbone: dinov2_vits14
backbone_repo: facebookresearch/dinov2
backbone_source: github
backbone_weights: null

epochs: 30
freeze_epochs: 5
batch_size: 8
tile_size: 512
lr_head: 0.001
lr_backbone: 0.0001
weight_decay: 0.0001
num_workers: 4
threshold: 0.5
seed: 42
accelerator: auto
devices: 1

mean: [0.430, 0.411, 0.296]
std: [0.213, 0.156, 0.143]
checkpoint_dir: checkpoint
models_dir: models
